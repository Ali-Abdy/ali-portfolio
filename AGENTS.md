# Portfolio development

Read the relevant version-matched Next.js guide in `node_modules/next/dist/docs/` before changing routing, layouts, metadata, or framework APIs.

- Keep German and English copy in `content/site.ts` and `content/projects.ts`.
- Preserve honest early-career positioning and explicit project statuses.
- Do not modify the separate Luxury Barbershop repository.
- Run lint, type checking, build, and browser tests after substantive changes.
